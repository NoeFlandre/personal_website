---
import Footer from "@/components/Footer.astro";
import Header from "@/components/Header.astro";
import { SITE } from "@/config";
import Layout from "./Layout.astro";

export interface Props {
  frontmatter: {
    title: string;
    description?: string;
  };
}

const { frontmatter } = Astro.props;

const outlineLinks = [
  { href: "#about-intro", label: "About" },
  { href: "#journey-map", label: "Journey Map" },
  { href: "#github-activity", label: "GitHub Activity" },
  { href: "#recommendations", label: "Recommendations" },
  { href: "#publications", label: "Publications" },
  { href: "#experience", label: "Experience" },
  { href: "#education", label: "Education" },
  { href: "#projects", label: "Projects" },
  { href: "#awards", label: "Awards" },
  { href: "#humanitarian-impact", label: "Humanitarian Impact" },
];
---

<Layout title={`${frontmatter.title} | ${SITE.title}`} description={frontmatter.description}>
  <Header />
  <main id="main-content" class="mx-auto w-full max-w-6xl px-4 pb-12">
    <div class="mt-8 grid gap-10 lg:grid-cols-[220px_minmax(0,1fr)]">
      <aside class="hidden lg:block">
        <nav class="about-outline" data-about-outline aria-label="About page sections">
          <p class="about-outline__label">On This Page</p>
          <ul class="about-outline__list">
            {outlineLinks.map((link) => (
              <li>
                <a href={link.href} class="about-outline__link" data-outline-link>
                  {link.label}
                </a>
              </li>
            ))}
          </ul>
        </nav>
      </aside>

      <div>
        <details class="about-outline-mobile lg:hidden" data-about-outline-mobile>
          <summary class="about-outline-mobile__summary">Jump To Section</summary>
          <ul class="about-outline-mobile__list">
            {outlineLinks.map((link) => (
              <li>
                <a href={link.href} class="about-outline__link" data-outline-link>
                  {link.label}
                </a>
              </li>
            ))}
          </ul>
        </details>

        <section id="about" class="prose mb-28 max-w-3xl prose-img:border-0">
          <h1 class="text-2xl tracking-wider sm:text-3xl">{frontmatter.title}</h1>
          <slot />
        </section>
      </div>
    </div>
  </main>
  <Footer />
</Layout>

<script is:inline>
  const initAboutOutline = () => {
    if (document.documentElement.dataset.aboutOutlineInit === "true") return;

    const links = Array.from(document.querySelectorAll("[data-outline-link]"));
    const sectionMap = new Map();

    links.forEach((link) => {
      const href = link.getAttribute("href");
      if (!href || !href.startsWith("#")) return;
      const section = document.getElementById(href.slice(1));
      if (!section) return;
      sectionMap.set(section.id, section);
    });

    const sections = Array.from(sectionMap.values());

    if (links.length === 0 || sections.length === 0) return;

    const setActive = (id) => {
      links.forEach((link) => {
        const isActive = link.getAttribute("href") === `#${id}`;
        link.dataset.active = isActive ? "true" : "false";
      });
    };

    const observer = new IntersectionObserver(
      (entries) => {
        const visible = entries
          .filter((entry) => entry.isIntersecting)
          .sort((a, b) => a.boundingClientRect.top - b.boundingClientRect.top);

        if (visible.length > 0) {
          setActive(visible[0].target.id);
        }
      },
      {
        rootMargin: "-35% 0px -55% 0px",
        threshold: [0, 1],
      },
    );

    sections.forEach((section) => observer.observe(section));
    setActive(sections[0].id);

    links.forEach((link) => {
      link.addEventListener("click", (event) => {
        const href = link.getAttribute("href");
        if (!href || !href.startsWith("#")) return;
        const section = document.getElementById(href.slice(1));
        if (!section) return;
        event.preventDefault();
        section.scrollIntoView({ behavior: "smooth", block: "start" });
        setActive(section.id);

        const mobileDropdown = link.closest("[data-about-outline-mobile]");
        if (mobileDropdown) {
          mobileDropdown.removeAttribute("open");
        }
      });
    });

    document.documentElement.dataset.aboutOutlineInit = "true";
  };

  if (document.readyState === "loading") {
    document.addEventListener("DOMContentLoaded", initAboutOutline, { once: true });
  } else {
    initAboutOutline();
  }
  document.addEventListener("astro:page-load", initAboutOutline);
</script>

<style>
  .about-outline {
    position: sticky;
    top: 5.5rem;
    border-left: 2px solid color-mix(in srgb, var(--border) 78%, transparent);
    padding-left: 0.85rem;
  }

  .about-outline__label {
    margin: 0 0 0.7rem;
    font-size: 0.72rem;
    font-weight: 700;
    letter-spacing: 0.12em;
    text-transform: uppercase;
    color: color-mix(in srgb, var(--foreground) 62%, transparent);
  }

  .about-outline__list {
    margin: 0;
    padding: 0;
    list-style: none;
    display: grid;
    gap: 0.25rem;
  }

  .about-outline__link {
    display: inline-flex;
    width: 100%;
    border-radius: 0.4rem;
    padding: 0.28rem 0.45rem;
    color: color-mix(in srgb, var(--foreground) 85%, transparent);
    text-decoration: none;
    transition: background-color 140ms ease, color 140ms ease, transform 140ms ease;
  }

  .about-outline__link:hover {
    color: var(--foreground);
    background: color-mix(in srgb, var(--muted) 82%, transparent);
    transform: translateX(2px);
  }

  .about-outline__link[data-active="true"] {
    color: var(--background);
    background: var(--accent);
    font-weight: 700;
  }

  .about-outline-mobile {
    margin-bottom: 1rem;
    border: 1px solid color-mix(in srgb, var(--border) 75%, transparent);
    border-radius: 0.7rem;
    background: color-mix(in srgb, var(--background) 90%, var(--muted));
    overflow: hidden;
  }

  .about-outline-mobile__summary {
    list-style: none;
    cursor: pointer;
    user-select: none;
    padding: 0.7rem 0.9rem;
    font-size: 0.9rem;
    font-weight: 700;
  }

  .about-outline-mobile__summary::-webkit-details-marker {
    display: none;
  }

  .about-outline-mobile__summary::after {
    content: "▾";
    float: right;
    opacity: 0.7;
    transform: translateY(-1px);
  }

  .about-outline-mobile[open] .about-outline-mobile__summary::after {
    content: "▴";
  }

  .about-outline-mobile__list {
    margin: 0;
    padding: 0.25rem 0.6rem 0.7rem;
    list-style: none;
    display: grid;
    gap: 0.25rem;
  }
</style>
